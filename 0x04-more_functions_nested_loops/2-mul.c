#include "main.h"

/**
 * mul - multiplies the two input integers
 * @a: the first parameter to be operated on
 * @b: the second parameter to be operated on
 *
 * Return: it returns an integer.
 */
int mul(int a, int b)
{
	return (a * b);
}
